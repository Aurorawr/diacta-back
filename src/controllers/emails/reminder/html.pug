doctype html
html
    head
        block head
            meta(charset="utf-8")
            meta(name="viewport", content="width=device-width")
            meta(http-equiv="X-UA-Compatible", content="IE=edge")
            meta(name="x-apple-disable-message-reformatting")
            link(rel="preconnect", href="https://fonts.googleapis.com")
            link(rel="preconnect", href="https://fonts.gstatic.com", 'crossorigin')
            link(href="https://fonts.googleapis.com/css2?family=Roboto&display=swap", rel="stylesheet") 
            style.
                @media only screen and (max-width: 800px) {
                    .container {
                        width: 100% !important;
                    }
                }
                body {
                    font-family: 'Roboto', sans-serif;
                }
                .container {
                    width: 50%;
                    margin: 0 auto;
                }
                .header {
                    background-color: #3f51b5;
                    padding: 2rem;
                    text-align: center;
                    color: #fff;
                    font-size: 20px;
                }
                .message {
                    font-size: 24px;
                }
                .logo {
                    width: 50px;
                }
                .check-button {
                    background-color: #3f51b5;
                    color: #fff;
                    padding: 1rem;
                    font-size: 16px;
                    border-radius: 8px;
                    border: none;
                }
                .title-header {
                    display: inline-block;
                }
    body
        div.container
            div.header
                img.logo(src="https://diacta.herokuapp.com/assets/img/logoActas.png")
                h2.title-header Actas Banco de Tiempo de San Miguel
            h1 Hola #{name}
            p.message #{message}
            - const actionUrl = #{url}
            if actionUrl
                a(href=actionUrl)
                    button.check-button Revisar